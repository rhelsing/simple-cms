<% @page_title = @page.name %>

<h2><%= @page.name %></h2>

<% @page.sections.visible.sorted.each do |section| %>
  <div class="section">
    <% case section.content_type %>
    <% when 'HTML' %>
      <%= raw(sanitize(section.context, :tags => ['strong', 'em', 'a'])) %>
    <% when 'text' %>
      <p><%= section.context %></p>
    <% end %>
  </div>
<% end %>
